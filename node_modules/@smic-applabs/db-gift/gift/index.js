const {
  dynamicSchemaImport,
  dynamicModelImport,
  getModel
} = require("../utils")

module.exports = {
  getSchemas: dynamicSchemaImport(`${__dirname}/schemas`),
  getModels: dynamicModelImport(`${__dirname}/models`),
  getModel
}