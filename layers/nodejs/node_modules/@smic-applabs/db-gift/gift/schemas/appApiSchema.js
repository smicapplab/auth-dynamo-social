const getSchemas = Schema => {
    const AppApiSchema = new Schema({
      name: String,
      appSecret: String,
      appAlias: String,
      status: { type: String, default: "Active" },
    })

    return {
      AppApiSchema,
    }
  }
  
module.exports = getSchemas