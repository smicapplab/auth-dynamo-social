const getSchemas = Schema => {
    const SmsLogSchema = new Schema({
      mobile: String,
      message: String,
      response: JSON
    })

    return {
      SmsLogSchema
    }
  }
  
module.exports = getSchemas